# ARTART 에디터 허브 고정 입구

에디터에게 공유하는 주소를 한 곳으로 고정하기 위한 공개 연결 페이지입니다.

- 비밀번호, 작업 파일, 제작 코드 또는 API 키는 들어 있지 않습니다.
- `target.json`에는 현재 정상 확인된 Cloudflare Quick Tunnel 주소만 기록됩니다.
- 허브의 `tunnel-guardian.js`가 주소 변경 때만 `target.json`을 갱신합니다.

